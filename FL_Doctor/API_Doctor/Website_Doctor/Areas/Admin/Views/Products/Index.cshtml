@using Website_Doctor.Areas.Admin.Data
@model IQueryable<Product>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="right_col" role="main">
    <div class="">
        <div class="page-title">
            <div class="title_left">
                <h3>Quản lý chuyên khoa khám bệnh</h3>
            </div>
            <div class="title_right">
                <div class="col-md-5 col-sm-5 col-xs-12 row pull-right top_search">
                    <div class="input-group">
                        <input type="text" class="form-control" placeholder="Search for...">
                        <span class="input-group-btn">
                            <button class="btn btn-default" type="button">Go!</button>
                        </span>
                    </div>
                </div>
            </div>
        </div>
        <div class="clearfix"></div>
        <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                    <div class="x_title">
                        <h2>Danh sách chuyên khoa khám bệnh</h2>
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <table id="datatable" class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>STT</th>
                                    <th>Tên chuyên khoa</th>
                                    <th>Mã chuyên khoa</th>
                                    <th>Phí khám/chữa bệnh</th>
                                    <th>Ngày tạo</th>
                                    <td>Trạng thái</td>
                                    <th></th>
                                </tr>
                            </thead>

                            <tbody>
                                @if (Model.Count() > 0)
                                {
                                    var i = 0;
                                    foreach (var item in Model)
                                    {
                                        i++;
                                        <tr>
                                            <td>@i</td>
                                            <td>@item.name</td>
                                            <td>@item.code</td>
                                            <td>@item.price</td>
                                            <td>@item.dateCreate</td>
                                            <td>
                                                @if (item.isActive == true)
                                                {
                                                    <span class="btn btn-success btn-xs"><i class="fa fa-check"></i> Active</span>
                                                }
                                                else
                                                {
                                                    <span class="btn btn-danger btn-xs"><i class="fa fa-times"></i> De-active</span>
                                                }
                                            </td>
                                            <td>
                                                <a data-toggle="modal" data-target="#acc-@item.id">
                                                    <span class="btn btn-primary btn-xs"><i class="fa fa-eye"></i> View</span>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="7">Không có dữ liệu.</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Modal -->
<div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog">
        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Thông báo</h4>
            </div>
            <div class="modal-body">
                <p>@TempData["MsgErr"]</p>
            </div>
        </div>

    </div>
</div>
@section scripts{
    <script>
        /****************************************
         *       Basic Table                   *
         ****************************************/
        $('#datatable').DataTable();
        if (!!@TempData["MsgErr"]) {
            $("#myModal").modal()
        }
    </script>
}